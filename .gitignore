# Dependencies
node_modules/

# Local editor/tooling
.claude/

# Regenerable artifacts & stale backups — NOT committed, will NOT transfer via git clone.
# Regenerate with the tools in scripts/, or copy archive/ manually if you want them on the new machine.
archive/
test_screenshots/
lighthouse-*

# OS files
.DS_Store
Thumbs.db
